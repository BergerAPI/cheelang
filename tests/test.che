const neededForDrip = 150; const bad = 1

fn narutoDripCheck(dripValue, maxDrip) {
    const percentage = ((dripValue / maxDrip) * 100)

    println("Naruto's drip level is: " + percentage + "% - bad is " + bad)
}

narutoDripCheck(125, neededForDrip)